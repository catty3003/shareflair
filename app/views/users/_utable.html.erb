<div class="bs-docs-section">

  
              
  <div class="bs-example table-responsive">
    <table class="table table-striped table-bordered table-hover ">
        <thead>
            <tr>
                <th>User</th>
                <th>Gender</th>
                <th>Typical Size</th>
                <th>City</th>
                <th>Products</th>

                
            </tr>
        </thead>


        <tbody>

          <% @users.each do |u| %>
            <tr>              
              
              <td width="20%"><h4><%= link_to u.username, users_profile_path(id: u.id)  %><h4></td>
              <td width="20%"><%= u.gender %></td>
              <td width="20%"><%= u.size %></td>
              <td width="20%"><%= u.city %></td>
              <td width="20%"><%= link_to pluralize(u.products.count, "Product"),  users_profile_path(id: u.id)  %></td>
              
             <% end %>
        </tbody>
    </table>
  </div>
</div>
  




